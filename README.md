# vy-vue3-project


```javasc
project-name（根目录）
	|-- public（公共目录）
	|-- src（源代码）
		|-- config (顶级配置，存放通用配置)
		|-- packages（基础设施组件）
		|-- webapp（Web应用）
			|-- admin（管理后台）
				|-- api（API 接口目录）
				|-- assets（静态资源目录）
				|-- config（配置）
				|-- layouts（布局）
				|-- router（路由）
				|-- views（视图）
				|-- index.ts
			|-- 前台
			|-- 地图
		|-- App.vue（App）
		|-- axios.ts（公共axios）
		|-- main.ts（main）
		|-- router.ts（公共router）
```